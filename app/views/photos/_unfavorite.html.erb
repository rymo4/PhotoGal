<%= form_for current_user.favorites.find_by_photo_id(@photo),
             :html => { :method => :delete },
             :remote => true do |f| %>
  <div class="actions"><%= f.submit "Unfavorite" %></div>
<% end %>